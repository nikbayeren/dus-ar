[build]
  publish = "."
  command = ""

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# If you use Netlify, set the publish directory and build command accordingly.
# Example for a simple static site (no build step):
# [build]
#   publish = "."
#   command = ""
